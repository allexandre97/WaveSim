#include <vector>

using namespace std;

void TwoWaySolver(int NSteps,
                  int NOut,
                  std::string OutName,
                  vector<vector<float>>& STATE,
                  int DIMENSION,
                  float C);